import './header.css';
import Menu from '../Menu/Menu';

const Header = () => {
    return (
        <div className="header-container">
            <div>Title</div>
            <Menu />
        </div>
    )
};

export default Header;